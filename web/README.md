# Web Panel

Vue3 + Element Plus management panel (built in a later task).

Will include: login page, node table (online status / accumulated upstream /
last seen), "new node" dialog that generates a copyable install command, policy
editor (interval / size / enabled toggle), and basic traffic stats.
